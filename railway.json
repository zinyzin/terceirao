{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "preDeployCommand": "if [ -d prisma/migrations ] && [ \"$(ls -A prisma/migrations 2>/dev/null | wc -l)\" -gt 0 ]; then npx prisma migrate deploy; else npx prisma db push; fi && node src/seed.js",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
